<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Process_Audit_Required_Fields</fullName>
    <active>true</active>
    <errorConditionFormula>IF( 
   AND( 
		ISPICKVAL(Was_the_Process_Followed__c,&apos;Yes&apos;),
        OR(	ISBLANK(Was_the_process_followed_effectively__c),
			ISBLANK(Could_anything_have_been_done_better__c),
			ISBLANK(Team_member_s_perception_effectivenes__c)
            ) 
      ), 
TRUE,
IF( 
   AND( 
      ISPICKVAL(Was_the_Process_Followed__c,&apos;No&apos;), 
         OR(ISBLANK(What_steps_were_missed__c),
			ISBLANK(Why_do_you_feel_they_were_missed_skill__c),
			ISBLANK(Why_did_employee_use_alt_approach__c)
            ) 
      ), 
 TRUE,
FALSE))</errorConditionFormula>
    <errorMessage>Please fill out the required fields under the Yes or No section.</errorMessage>
</ValidationRule>
